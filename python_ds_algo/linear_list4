#데이터 삭제

kakaotalk = ['가을','레이','리즈','원영','이서']
#데이터 삭제 함수 delete_data 선언
def delete_data(position): #삭제할 위치를 매개변수로 받음
  #범위 초과
  if position < 0 or position >= len(kakaotalk): 
    print("데이터를 삭제할 범위를 벗어났습니다.")
    return
  
  k_len = len(kakaotalk) #현재 배열 계산
  kakaotalk[position] = None # position에 해당하는 데이터 삭제
  """position +1부터 해당 인덱스 값을 왼쪽으로 이동시킴
    + 이동한 값의 인덱스를 빈칸으로 만듬"""
  for i in range(position + 1, k_len):
    kakaotalk[i-1] = kakaotalk[i]
    kakaotalk[i] = None
    
  del(kakaotalk[k_len-1]) #맨 뒤 데이터는 삭제
  #테스트용 코드
delete_data(1) # ['가을','리즈','원영','이서']
print(kakaotalk)
delete_data(3) #['가을','레이','리즈','이서']
print(kakaotalk)
  