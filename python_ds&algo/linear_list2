#선형 리스트를 생성하는 함수
kakaotalk = []

def add_data(friend): #매개변수로 저장할 데이터를 전달받음.
  
  kakaotalk.append(None) #1.빈 배열에 빈칸 추가(호출 시 매번)
  k_len = len(kakaotalk) #2. 배열의 현재 크기를 k_len에 저장
  kakaotalk[k_len-1] = friend #3.맨 뒤에 전달받은 매개변수를 대입
  
add_data('가을')
add_data('원영')
add_data('레이')
add_data('유진')
add_data('이서')
add_data('리즈')

print(kakaotalk)